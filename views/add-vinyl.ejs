<%- include('partials/header') %>

    <script src="https://unpkg.com/html5-qrcode" type="text/javascript"></script>

    <div class="max-w-screen-lg mx-auto pb-20 px-4">

        <div class="text-center mb-8 md:mb-12 pt-4 md:pt-8">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-primary-theme/10 text-primary-theme mb-4">
                <i class="fa-solid fa-music text-3xl"></i>
            </div>
            <h1 class="text-3xl md:text-4xl font-black"><%= t('add_vinyl.title') %></h1>
            <p class="opacity-60 mt-2"><%= t('add_vinyl.desc') %></p>
        </div>

        <form action="<%= baseUrl %>/search-discogs" method="POST" class="max-w-2xl mx-auto mb-10">
            <input type="hidden" name="type" value="vinyl">

            <div class="flex flex-col sm:flex-row gap-3">
                <div class="flex-grow relative group">
                    <div class="absolute inset-y-0 left-0 flex items-center pl-4 pointer-events-none z-10">
                        <i class="fa-solid fa-search opacity-50 group-focus-within:text-primary-theme transition-colors"></i>
                    </div>
                    <input type="text" name="query" value="<%= locals.searchQuery || '' %>"
                        class="block w-full p-4 pl-12 text-base bg-black/5 dark:bg-white/5 border border-transparent focus:border-primary-theme rounded-2xl focus:ring-2 focus:ring-primary-theme/50 placeholder-opacity-50 transition-all outline-none"
                        placeholder="<%= t('add_vinyl.search_placeholder') %>" required autofocus>
                </div>

                <button type="submit"
                    class="bg-primary-theme hover:brightness-110 text-white font-bold px-8 py-4 rounded-2xl shadow-lg shadow-primary-theme/20 transition-all active:scale-95 flex items-center justify-center gap-2 whitespace-nowrap">
                    <i class="fa-solid fa-search"></i>
                    <%= t('common.search') %>
                </button>
            </div>

            <!-- Advanced Filters -->
            <div class="mt-4 flex justify-center">
                <button type="button" onclick="document.getElementById('advanced-filters').classList.toggle('hidden'); document.getElementById('filter-icon').classList.toggle('fa-chevron-up'); document.getElementById('filter-icon').classList.toggle('fa-chevron-down')" class="text-xs opacity-50 hover:opacity-100 flex items-center gap-2 transition-opacity font-bold uppercase tracking-wider">
                    <i id="filter-icon" class="fa-solid fa-chevron-down"></i> <%= t('add_vinyl.advanced_filters') || 'Filtres avancés' %>
                </button>
            </div>
            
            <div id="advanced-filters" class="hidden mt-4 p-4 rounded-2xl border border-black/10 dark:border-white/10 bg-black/5 dark:bg-white/5 grid grid-cols-2 md:grid-cols-4 gap-4">
                <div>
                    <label class="block text-[10px] font-black uppercase opacity-40 mb-1"><%= t('add_vinyl.filter_year') || 'Année' %></label>
                    <input type="number" name="year" class="w-full p-3 text-sm rounded-xl bg-white dark:bg-black/50 border border-transparent focus:ring-2 focus:ring-primary-theme outline-none" placeholder="<%= t('add_vinyl.filter_ex_year') || 'Ex: 1994' %>">
                </div>
                <div>
                    <label class="block text-[10px] font-black uppercase opacity-40 mb-1"><%= t('add_vinyl.filter_country') || 'Pays' %></label>
                    <input type="text" name="country" class="w-full p-3 text-sm rounded-xl bg-white dark:bg-black/50 border border-transparent focus:ring-2 focus:ring-primary-theme outline-none" placeholder="<%= t('add_vinyl.filter_ex_country') || 'Ex: UK' %>">
                </div>
                <div>
                    <label class="block text-[10px] font-black uppercase opacity-40 mb-1"><%= t('add_vinyl.filter_genre') || 'Genre' %></label>
                    <input type="text" name="genre_filter" class="w-full p-3 text-sm rounded-xl bg-white dark:bg-black/50 border border-transparent focus:ring-2 focus:ring-primary-theme outline-none" placeholder="<%= t('add_vinyl.filter_ex_genre') || 'Ex: Electronic' %>">
                </div>
                <div>
                    <label class="block text-[10px] font-black uppercase opacity-40 mb-1"><%= t('add_vinyl.filter_label') || 'Label' %></label>
                    <input type="text" name="label_filter" class="w-full p-3 text-sm rounded-xl bg-white dark:bg-black/50 border border-transparent focus:ring-2 focus:ring-primary-theme outline-none" placeholder="<%= t('add_vinyl.filter_ex_label') || 'Ex: EMI' %>">
                </div>
            </div>

            <div class="flex items-center gap-3 mt-6 justify-center">
                <span class="text-xs font-bold uppercase opacity-30"><%= t('add_vinyl.separator_or') %></span>
            </div>

            <div class="flex justify-center mt-3">
                <button id="start-scan" type="button"
                    class="bg-black/5 dark:bg-white/5 hover:bg-primary-theme hover:text-white px-6 py-3 rounded-xl font-bold text-sm transition-all flex items-center gap-2 active:scale-95">
                    <i class="fa-solid fa-barcode"></i>
                    <span>
                        <%= t('add_vinyl.btn_scan') %>
                    </span>
                </button>
            </div>

            <div id="reader"
                class="hidden mt-6 w-full rounded-2xl overflow-hidden border border-black/10 dark:border-white/10 shadow-lg">
            </div>
        </form>

        <% if (locals.results) { %>
            <div class="mb-4 flex items-center justify-between">
                <h2 class="text-xl font-bold transition-colors">
                    <%= t('add_vinyl.results_title') %>
                </h2>
                <span
                    class="bg-black/10 dark:bg-white/10 text-xs font-mono px-2 py-1 rounded opacity-70 transition-colors">
                    <%= results.length %>
                </span>
            </div>

            <% if (results.length===0) { %>
                <div
                    class="text-center py-12 card-theme bg-opacity-50 rounded-xl border-dashed opacity-70 transition-colors">
                    <i class="fa-solid fa-ghost text-4xl mb-3 opacity-50"></i>
                    <p>
                        <%= t('add_vinyl.no_results') %>
                    </p>
                </div>
                <% } else { %>
                    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                        <% results.forEach(item=> { %>
                            <a href="<%= baseUrl %>/confirm-vinyl/<%= item.id %><%= locals.searchType ? '?type=' + locals.searchType : '' %>"
                                class="card-theme rounded-xl overflow-hidden hover:border-primary-theme transition-all group flex flex-col hover:shadow-lg hover:-translate-y-1">
                                <div class="relative aspect-square bg-black/5 dark:bg-white/5">
                                    <% if(item.cover_image && !item.cover_image.includes('spacer.gif')) { %>
                                        <img src="<%= item.cover_image %>" class="w-full h-full object-cover">
                                        <% } else { %>
                                            <div class="w-full h-full flex items-center justify-center opacity-30">
                                                <i class="fa-solid fa-compact-disc text-3xl"></i>
                                            </div>
                                            <% } %>
                                </div>

                                <div class="p-3 flex flex-col flex-grow">
                                    <h3 class="font-bold text-sm leading-tight mb-1 line-clamp-2 transition-colors">
                                        <%= item.title %>
                                    </h3>

                                    <p class="opacity-60 text-[10px] mb-2 transition-colors">
                                        <%= item.country ? item.country + ' • ' : '' %>
                                            <%= item.year %>
                                    </p>

                                    <% if(item.is_advanced_cd) { %>
                                        <div class="flex items-center gap-1 mb-2">
                                            <span
                                                class="text-[10px] font-bold text-white px-2 py-0.5 rounded-full shadow-md truncate max-w-full <%= item.is_advanced_cd === 'sacd' ? 'bg-red-600/90' : 'bg-blue-600/90' %>">
                                                <%= item.is_advanced_cd==='sacd' ? t('media.sacd') : t('media.cdr') %>
                                            </span>
                                        </div>
                                        <% } %>

                                            <% if(item.variant_info) { %>
                                                <div class="flex items-center gap-1 mb-3">
                                                    <span
                                                        class="w-2 h-2 rounded-full bg-primary-theme inline-block flex-shrink-0"></span>
                                                    <span
                                                        class="text-[10px] opacity-70 truncate max-w-full font-medium italic">
                                                        <%= item.variant_info %>
                                                    </span>
                                                </div>
                                                <% } %>

                                                    <div class="flex flex-wrap gap-1 mt-auto">
                                                        <% if(item.format) { %>
                                                            <% item.format.slice(0, 2).forEach(fmt=> { %>
                                                                <span
                                                                    class="text-[9px] bg-black/5 dark:bg-white/10 opacity-70 px-1.5 py-0.5 rounded border border-transparent truncate max-w-full transition-colors">
                                                                    <%= fmt %>
                                                                </span>
                                                                <% }) %>
                                                                    <% } %>
                                                    </div>
                                </div>
                            </a>
                            <% }) %>
                    </div>
                    <% } %>
                        <% } %>
    </div>

    <script>
        const scanTranslations = {
            btnDefault: '<i class="fa-solid fa-barcode text-primary-theme text-xl"></i> <span><%- JSON.stringify(t("add_vinyl.btn_scan")).slice(1, -1) %></span>',
            init: '<i class="fa-solid fa-spinner fa-spin text-xl"></i> <span><%- JSON.stringify(t("add_vinyl.scan_init")).slice(1, -1) %></span>',
            stop: '<i class="fa-solid fa-xmark text-xl"></i> <span><%- JSON.stringify(t("add_vinyl.scan_stop")).slice(1, -1) %></span>',
            found: '<i class="fa-solid fa-check text-xl"></i> <span><%- JSON.stringify(t("add_vinyl.scan_found")).slice(1, -1) %></span>',
            error: <%- JSON.stringify(t("add_vinyl.scan_error")) %>
        };

        const html5QrCode = new Html5Qrcode("reader");
        const scanButton = document.getElementById('start-scan');
        const readerDiv = document.getElementById('reader');
        const searchInput = document.querySelector('input[name="query"]');
        const form = document.querySelector('form[action="<%= baseUrl %>/search-discogs"]');
        let isScanning = false;

        scanButton.addEventListener('click', () => {
            if (isScanning) {
                html5QrCode.stop().then(() => {
                    readerDiv.classList.add('hidden');
                    scanButton.innerHTML = scanTranslations.btnDefault;

                    scanButton.classList.remove('bg-red-600', 'hover:bg-red-500', 'text-white');
                    scanButton.classList.add('bg-black/5', 'dark:bg-white/5', 'hover:bg-black/10', 'dark:hover:bg-white/10');

                    isScanning = false;
                }).catch(err => console.error("Erreur stop scan", err));
            } else {
                readerDiv.classList.remove('hidden');
                scanButton.innerHTML = scanTranslations.init;

                const config = {
                    fps: 10,
                    qrbox: { width: 250, height: 150 },
                    aspectRatio: 1.0,
                    formatsToSupport: [
                        Html5QrcodeSupportedFormats.EAN_13,
                        Html5QrcodeSupportedFormats.UPC_A,
                        Html5QrcodeSupportedFormats.UPC_E
                    ]
                };
                html5QrCode.start(
                    { facingMode: "environment" },
                    config,
                    (decodedText) => {
                        html5QrCode.stop().then(() => {
                            readerDiv.classList.add('hidden');
                            scanButton.innerHTML = scanTranslations.found;
                            searchInput.value = decodedText;
                            form.submit();
                        });
                    },
                    (errorMessage) => { }
                ).then(() => {
                    isScanning = true;
                    scanButton.innerHTML = scanTranslations.stop;

                    scanButton.classList.remove('bg-black/5', 'dark:bg-white/5', 'hover:bg-black/10', 'dark:hover:bg-white/10');
                    scanButton.classList.add('bg-red-600', 'hover:bg-red-500', 'text-white');
                }).catch(err => {
                    console.error("Erreur start scan", err);
                    scanButton.innerHTML = scanTranslations.error;
                });
            }
        });
    </script>

    <%- include('partials/footer') %>
